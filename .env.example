VITE_GITHUB_TOKEN=
# Example env for local development
# Create a personal access token on GitHub and add it here to increase API rate limits.
# Never commit real tokens to git. Add your token to .env (not committed).
VITE_GITHUB_TOKEN=
